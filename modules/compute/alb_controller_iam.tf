# Fetch the latest AWS-published IAM policy for the ALB controller
data "http" "alb_controller_policy" {
  url = "https://raw.githubusercontent.com/kubernetes-sigs/aws-load-balancer-controller/main/docs/install/iam_policy.json"
}

resource "aws_iam_policy" "alb_controller" {
  #checkov:skip=CKV_AWS_290:AWS-published policy contains write operations on ALB/NLB/SG resources by design
  #checkov:skip=CKV_AWS_355:Same; required for controller to create and modify load balancer resources
  name        = "${var.project}-alb-controller"
  description = "AWS Load Balancer Controller IAM policy (AWS-published)"
  policy      = data.http.alb_controller_policy.response_body
}

# IRSA trust policy: the OIDC provider can assume this role
# when the service account aws-load-balancer-controller in kube-system requests it
data "aws_iam_policy_document" "alb_controller_assume" {
  statement {
    actions = ["sts:AssumeRoleWithWebIdentity"]

    principals {
      type        = "Federated"
      identifiers = [aws_iam_openid_connect_provider.cluster.arn]
    }

    condition {
      test     = "StringEquals"
      variable = "${replace(aws_iam_openid_connect_provider.cluster.url, "https://", "")}:sub"
      values   = ["system:serviceaccount:kube-system:aws-load-balancer-controller"]
    }

    condition {
      test     = "StringEquals"
      variable = "${replace(aws_iam_openid_connect_provider.cluster.url, "https://", "")}:aud"
      values   = ["sts.amazonaws.com"]
    }
  }
}

resource "aws_iam_role" "alb_controller" {
  name               = "${var.project}-alb-controller"
  assume_role_policy = data.aws_iam_policy_document.alb_controller_assume.json
}

resource "aws_iam_role_policy_attachment" "alb_controller" {
  role       = aws_iam_role.alb_controller.name
  policy_arn = aws_iam_policy.alb_controller.arn
}